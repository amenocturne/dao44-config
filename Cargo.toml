[package]
name = "dao44-config"
version = "0.1.0"
edition = "2024"
description = "Typed Dao44 keyboard layout generator and live preview"
license = "MIT"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
