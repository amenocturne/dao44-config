# Keep the Dao radio power setting from the maintained upstream config.
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# The physical N switch exceeded ZMK's 5 ms default often enough to double-type.
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=20
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=20
